platform: linux

image_resource:
  type: docker-image
  source:
    repository: "bravissimolabs/alpine-git"

inputs:
- name: flight-school

run:
  path: git
  args: ['rev-parse', 'HEAD']
  dir: 'flight-school'
