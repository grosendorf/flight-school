platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: 2.3.0

inputs:
- name: flight-school
- name: version
outputs:
- name: build
- name: version

run:
  path: flight-school/ci/build.sh
